package sv.edu.udb.cfc.academic.enums;

/** Ciclo de vida: PROGRAMADO → EN_CURSO → FINALIZADO (o CANCELADO desde los dos primeros). */
public enum EstadoCurso {
    PROGRAMADO,
    EN_CURSO,
    FINALIZADO,
    CANCELADO
}